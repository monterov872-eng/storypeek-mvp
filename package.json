{
  "name": "storypeek-mvp",
  "private": true,
  "scripts": {
    "dev": "concurrently -k -n backend,mobile -c blue,green \"npm run dev:backend\" \"npm run dev:mobile:wait\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:mobile": "npm run start --prefix mobile",
    "dev:mobile:wait": "wait-on http-get://localhost:3001/health -t 60000 && npm run dev:mobile",
    "dev:android": "concurrently -k -n backend,mobile -c blue,green \"npm run dev:backend\" \"wait-on http-get://localhost:3001/health -t 60000 && npm run android --prefix mobile\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "wait-on": "^8.0.1"
  }
}
